<!--  app/views/devise/sessions/verify_otp.html.erb -->
<%= form_with(url: users_verify_otp_path, method: :post) do |f| %>
  <div class="field">
    <%= f.label :otp %>
    <%= f.text_field :otp %>
  </div>
  <div class="actions">
    <%= f.submit "Verify OTP" %>
  </div>
<% end %>
 