<!DOCTYPE html>
<html>
  <head>
    <title>TestProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%#= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
    <h3>
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
      <p class="navbar-text float-right">
        
      </p>
    </h3>
    
   <% if !@hide_navigation_links %>
  <nav style="background-color: #333; color: white; padding: 10px;">
  <% if user_signed_in? %>
    <div style="display: inline-block; margin-right: 20px;">
      Logged in as <strong><%= current_user.email %></strong>.
    </div>
    <div style="display: inline-block; margin-right: 20px;">
      <%= link_to "Edit profile", edit_user_registration_path, class: "navbar-link", style: "color: white; text-decoration: none;" %>
    </div>
    <div style="display: inline-block;">
      <%= button_to :logout, destroy_user_session_path, method: :delete, class: "navbar-link", style: "color: white; text-decoration: none; background-color: transparent; border: none; cursor: pointer;" %>
    </div>
  <% else %>
     <% if !@hide_navigation_links || !current_page?(users_confirmations_verify_otp_path) %>
      <div style="display: inline-block; margin-right: 20px;">
        <%= link_to "Sign up", new_user_registration_path, class: "navbar-link", style: "color: white; text-decoration: none;" %>
      </div>
      <div style="display: inline-block;">
        <%= link_to "Login", new_user_session_path, class: "navbar-link", style: "color: white; text-decoration: none;" %>
      </div>
    <% end %>
  <% end %>
</nav>
<% end %>
    <%= yield %>
    </div>
  </body>
</html>
